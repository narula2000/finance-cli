# Finance TUI

A fast, keyboard-driven personal finance manager built with C++20, FTXUI, CLI11, SQLite and SQLiteCpp.

Manage your finances directly from the terminal without leaving your keyboard.

## Features

- 📊 Terminal-based user interface
- 💰 Track income and expenses
- 🧾 Manage financial transactions
- 🏦 Manage accounts
- 📅 View transaction history
- 📈 Financial summaries and statistics
- 💾 Local SQLite database
- ⌨️ Keyboard-driven navigation
- 🚀 Lightweight and fast
- ⚠️ Strict compiler warnings

## Tech Stack

- **C++20** — Core application
- **FTXUI** — Terminal user interface
- **CLI11** — Command-line argument parsing
- **SQLite** — Local data storage
- **SQLiteCpp** — SQLite wrapper for clean usage
- **CMake** — Build system

## Requirements

- C++20-compatible compiler
- CMake 3.20+
- Git

## Building

1. Clone the repository:

   ```sh
   git clone https://github.com/narula2000/finance-tui.git
   cd finance-tui
   ```

2. Configure the project:

   ```sh
   cmake -S . -B build
   ```

3. Build:

   ```sh
   cmake --build build
   ```

4. Run:

   ```sh
   ./build/finance-tui
   ```

5. Show CLI options:

   ```sh
   ./build/finance-tui --help
   ```

## Compiler Warnings

The project is built with strict compiler warnings:

- `-Wall`
- `-Wextra`
- `-Wpedantic`
- `-Werror`

`compile_commands.json` is also generated automatically by CMake.

## Project Structure

```
finance-tui/
├── CMakeLists.txt
├── README.md
├── include/
│   └── ...
├── src/
│   └── main.cpp
└── build/
```

The `build/` directory is generated by CMake and should not be committed to Git.

## Data Storage

Finance data is stored locally using SQLite.

No external service or account is required. Your financial data stays on your machine.

## Roadmap

- [ ] SQLite database integration
- [ ] Transaction management
- [ ] Account management
- [ ] Income and expense tracking
- [ ] Budget management
- [ ] Monthly summaries
- [ ] Spending statistics
- [ ] Search and filtering
- [ ] CSV import/export
- [ ] Configuration file
- [ ] Improved keyboard shortcuts
- [ ] Themes and customization

## License

This project is licensed under the MIT License. See [LICENSE](LICENSE) for details.

## Status

🚧 Early Development

The project is currently under active development. The UI, API, database schema, and project structure may change.
